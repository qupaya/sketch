import { ClickBackdropDirective } from './click-backdrop.directive';

describe('DialogDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickBackdropDirective();
    expect(directive).toBeTruthy();
  });
});
